/**
 * Tailwind CSS v4 is a PostCSS plugin — there is no tailwind.config.js.
 * All theme configuration lives in app/globals.css under @theme.
 */
const config = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};

export default config;
